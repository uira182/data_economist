# Copie este ficheiro para .env na RAIZ do projeto (c:\BAU\data_economist\.env)
# e preencha com o seu token do PyPI. O ficheiro .env não é commitado (está no .gitignore).

# Token de API do PyPI (Account → API tokens em https://pypi.org/manage/account/)
# Use como username: __token__ e como password: o token
TWINE_USERNAME=__token__
TWINE_PASSWORD=pypi-AgEIc...seu_token_aqui
